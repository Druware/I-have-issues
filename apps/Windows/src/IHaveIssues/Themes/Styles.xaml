<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:kit="clr-namespace:IssuesKit;assembly=IssuesKit"
                    xmlns:p="clr-namespace:IHaveIssues.Presentation">

    <!--
        Theme brushes are referenced with DynamicResource throughout. They come from WPF's built-in
        Fluent theme, so they follow the OS light/dark and high-contrast setting at runtime — a
        StaticResource would bind the light-mode colour once and never update.
    -->

    <!-- MARK: Converters -->

    <p:NotEmptyToVisibilityConverter x:Key="NotEmptyToVisibility" />
    <p:CountToVisibilityConverter x:Key="CountToVisibility" />
    <p:NotNullToVisibilityConverter x:Key="NotNullToVisibility" />
    <p:NullToVisibilityConverter x:Key="NullToVisibility" />
    <p:FalseToVisibilityConverter x:Key="FalseToVisibility" />
    <p:SectionNameConverter x:Key="SectionName" />
    <p:IssueDateConverter x:Key="IssueDate" />
    <p:EnumDisplayNameConverter x:Key="EnumDisplayName" />
    <p:TitleOrPlaceholderConverter x:Key="TitleOrPlaceholder" />
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

    <!-- MARK: Typography -->

    <Style x:Key="CardTitleText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <Style x:Key="SectionHeaderText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,6" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <Style x:Key="SecondaryText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
    </Style>

    <Style x:Key="CaptionText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
    </Style>

    <Style x:Key="BodyText" TargetType="TextBlock">
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <!-- MARK: Surfaces -->

    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <Style x:Key="CommandBar" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource LayerFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <Setter Property="Padding" Value="8,6" />
    </Style>

    <!--
        Segoe Fluent Icons is the Windows 11 icon font; Segoe MDL2 Assets is the Windows 10
        fallback. Every icon button carries a ToolTip, so the action is still identifiable if
        neither font is present.
    -->
    <Style x:Key="IconButton" TargetType="Button">
        <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Width" Value="34" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <!-- MARK: Classification indicators -->

    <!--
        A filled dot tinted by issue type, echoing the Apple build's tinted SF Symbol. The tints are
        the Fluent system fill colours rather than fixed hex, so they stay legible in dark mode and
        under high contrast.
    -->
    <Style x:Key="TypeDot" TargetType="TextBlock">
        <Setter Property="Text" Value="&#x25CF;" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Type}" Value="{x:Static kit:IssueType.Bug}">
                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Type}" Value="{x:Static kit:IssueType.Feature}">
                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Type}" Value="{x:Static kit:IssueType.Task}">
                <Setter Property="Foreground" Value="{DynamicResource AccentTextFillColorPrimaryBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Type}" Value="{x:Static kit:IssueType.Question}">
                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PriorityText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Priority}" Value="{x:Static kit:IssuePriority.Low}">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Priority}" Value="{x:Static kit:IssuePriority.Medium}">
                <Setter Property="Foreground" Value="{DynamicResource AccentTextFillColorPrimaryBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Priority}" Value="{x:Static kit:IssuePriority.High}">
                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Priority}" Value="{x:Static kit:IssuePriority.Critical}">
                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="StatusText" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Status}" Value="{x:Static kit:IssueStatus.Open}">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Status}" Value="{x:Static kit:IssueStatus.InProgress}">
                <Setter Property="Foreground" Value="{DynamicResource AccentTextFillColorPrimaryBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Status}" Value="{x:Static kit:IssueStatus.Blocked}">
                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Status}" Value="{x:Static kit:IssueStatus.Resolved}">
                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- MARK: Forms -->

    <Style x:Key="FieldLabel" TargetType="TextBlock">
        <Setter Property="Margin" Value="0,0,0,4" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
    </Style>

    <Style x:Key="FormRow" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <Style x:Key="FormHelpText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,4,0,0" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
    </Style>

</ResourceDictionary>
